name: Reusable Workflow

on:
 workflow_dispatch
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - name: Checkout
     uses: actions/checkout@v4.2.0
   - name: Run Reusable Build Step
     run: echo 'This is a reusable workflow'
